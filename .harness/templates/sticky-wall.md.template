# 便签墙：{{feature_id}}

> 只追加、不修改已有便签；协调者负责关闭。每个阶段开始前读取本墙。

## 便签

| 便签 ID | 类型 | 提出者 | 阶段 | 内容 | 状态 |
| --- | --- | --- | --- | --- | --- |
| N1 | decision | {{author}} | {{stage}} | {{content}} | open |

## 类型

- `decision`：已确定的方案或口径
- `risk`：已识别的风险
- `question`：待确认问题
- `fact`：已验证事实
